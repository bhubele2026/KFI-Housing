{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "check:deploy-env": "tsx ./src/check-deploy-env.ts",
    "check:deployed-config": "tsx ./src/check-deployed-config.ts",
    "audit:fallback-attachments": "tsx ./src/audit-fallback-attachments.ts",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@workspace/db": "workspace:*",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "vitest": "^4.1.5"
  }
}
